services:
  postgres:
    image: postgres:16-alpine
    container_name: mission-control-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: missioncontrol
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mission-control-app
    restart: unless-stopped
    ports:
      - "3000:3000"
      - "5173:5173"
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/missioncontrol
      JWT_SECRET: ${JWT_SECRET:-dev-secret-change-in-production}
      NODE_OPTIONS: "--max-old-space-size=4096"
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy

  hermes-bridge:
    build:
      context: .
      dockerfile: Dockerfile.hermes-bridge
    container_name: mission-control-hermes-bridge
    restart: unless-stopped
    environment:
      DATABASE_URL: postgresql://postgres:postgres@postgres:5432/missioncontrol
      HERMES_INGEST_TOKEN: ${HERMES_INGEST_TOKEN:-test-token}
      AGENTIQ_API_URL: http://app:3000
      HERMES_BRIDGE_INTERVAL_SECONDS: ${HERMES_BRIDGE_INTERVAL_SECONDS:-10}
    volumes:
      - .:/app
    depends_on:
      postgres:
        condition: service_healthy
      app:
        condition: service_started
    command: ["sh", "-c", "cd /app && pnpm install && pnpm tsx apps/api/src/cli/hermes-bridge.ts"]

volumes:
  pgdata:
